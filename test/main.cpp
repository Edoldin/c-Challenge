#include <iostream>
 
int main(int argc, char *argv[])
{
    std::cout << "Init testing: " << std::endl;
    int fails = 0;

 
    return fails==0?EXIT_SUCCESS : EXIT_FAILURE; // optional return value
}